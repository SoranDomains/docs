---
title: "Testnet and contract addresses"
description: "Current contract addresses, SDK versions, endpoints and fees for integrating with Soran."
---

Soran is available on **Stellar testnet**. Mainnet is not available yet. The governed contract deployment and matching client releases are dated **10 September 2026**. The deployment manifest records the confirmed transactions and verification scope.

<span id="verified-claim-behavior" />

The current setup supports automatic username claims, G accounts with or without a memo, muxed M addresses and C contract destinations. Reverse and Primary lookup support the complete M address, including its routing ID.

For a working integration, start with the [quickstart](/quickstart) or try the [live examples](/live-examples).

## Network, versions and fees

| Setting | Value |
| --- | --- |
| Network | Stellar testnet |
| RPC | `https://soroban-testnet.stellar.org` |
| Network passphrase | `Test SDF Network ; September 2015` |
| Public namespace application | **5,000 XLM** upfront |
| Username claim | Free or a flat XLM price set by the namespace owner |

The namespace application fee goes into Allocator escrow. Award pays the configured recipient; rejection or stuck cancellation refunds 100%; withdrawal or expiry refunds 80%. See [namespace claim outcomes](/concepts/claiming-a-namespace).

A username fee goes directly to the owner's configured treasury when the claim succeeds. There is no username approval escrow or mandatory annual Soran ownership fee. Transaction and storage costs are separate. Read the namespace's current quote before showing a price; see [automatic claiming](/owner/automatic-claiming).

## SDK versions

Use these published versions together. Each supports Stellar SDK 17. Lookup reads names; Holder claims and manages them; Owner configures namespaces; MCP provides access for agents.

| Package | Current version |
| --- | --- |
| [Lookup](https://www.npmjs.com/package/@sorandomains/lookup) | **0.10.2** |
| [Holder](https://www.npmjs.com/package/@sorandomains/holder) | **0.7.0** |
| [Owner](https://www.npmjs.com/package/@sorandomains/owner) | **0.8.0** |
| [MCP](https://www.npmjs.com/package/@sorandomains/mcp) | **0.9.3** |

## Shared testnet contract addresses

**Universal Lookup is the shared read entry point for every namespace.** An integration does not need to maintain a list of namespace Resolvers.

```text Universal Lookup wrap
CDSORANQAJK35UV2HR63CMB6M5NYISHMUBTB6EQY2CZ3Y7HJDIOHRJWA
```

[View Lookup on Stellar Expert](https://stellar.expert/explorer/testnet/contract/CDSORANQAJK35UV2HR63CMB6M5NYISHMUBTB6EQY2CZ3Y7HJDIOHRJWA) · [Contract methods and examples](/api/onchain-resolution)

The other shared contracts handle namespace routing, G/C Primary elections and namespace applications:

<AccordionGroup>
  <Accordion title="Registry — namespace ownership and routing">
    ```text wrap
    CCSORANDPQINYOYB5SVO45WJP2LBBYKC72HHUIRVXB4J6RUZKDAUW7G4
    ```
    Governance can authorize code upgrades at this address. Use it with the Lookup above.
  </Accordion>
  <Accordion title="Primary — G/C display-name elections">
    ```text wrap
    CCSORAN7Y7ICQK2MBSVCJT3BUN5EHXKDKSTMGVB6QWSYXWMMLG2WIFJ6
    ```
    Read verified Primary names through Lookup. Exact-M elections are stored in Lookup itself.
  </Accordion>
  <Accordion title="Allocator — namespace applications and fee escrow">
    ```text wrap
    CCSORANYFHUJUWETEQ7UWIDN4JTPV7NN5G6YUSSBZI57X7EQWVA63VMJ
    ```
    This contract handles namespace applications, objections and settlement.
  </Accordion>
</AccordionGroup>

The [deployment manifest](https://raw.githubusercontent.com/SoranDomains/docs/main/reference/deployments/testnet.json) contains all six contract IDs, Wasm hashes, deployment transactions, ledgers, governance and namespace-fee configuration. Use addresses from this one deployment together. A `SORAN` prefix alone does not authenticate a contract.

## Nova namespace contracts

Nova is a working example namespace used by the [claim app](https://nova-claim-app.throbbing-cloud-0f8e.workers.dev/). These two contracts serve **only `.nova`**. For another namespace, discover its current pair through Lookup.

<AccordionGroup>
  <Accordion title="Nova Registrar — name issuance and claim rules">
    ```text wrap
    CDSORANBRJUG4LLF2D26JZIKRGLKHVWHPEGRR4ZJKKLBSREEADD4MDC2
    ```
  </Accordion>
  <Accordion title="Nova Resolver — payment destinations and profile records">
    ```text wrap
    CDSORANVZ5JS2VSVQ6VBYGE6A26NC736SL66HW7LZ7O4EE7BSMMDWFAX
    ```
  </Accordion>
</AccordionGroup>

Nova uses governed upgrades and its name policy is reclaimable. Example records can change; testnet names are demonstrations, not promises of permanent ownership.

<span id="lookup-muxed-identity-upgrade" />

## Contract interface versions

| Interface | Version |
| --- | --- |
| Lookup `version()` and `destination_version()` | `2` |
| Lookup `muxed_identity_version()` | `1` |
| Lookup `name_status_version()` | `1` |
| Lookup `batch_read_version()` | `3` |
| Lookup `batch_read_limit()` | `32` namespace identities |
| Lookup `primary_batch_limit()` | `16` Primary identities |
| Resolver `payment_version()` and `destination_version()` | `2` |
| Registrar `claim_version()` | `1` |
| Resolver `initialization_version()` | `1` |
| Allocator `ruling_version()` | `1` |
| Allocator `reopen_version()` | `1` |
| Registry namespace deployment salt | `1` |

Lookup's muxed identity extension is active at the same contract address. See the [full-M methods](/api/onchain-resolution#muxed-identity-extension) and [storage schema](/reference/contract-storage#muxed-identity-storage).

### Name status and history reads

The [9 September Lookup upgrade](https://stellar.expert/explorer/testnet/tx/34ac6a1189a83f6ea33692b4a9dca65324e147653b9ef8d74800c6eedc7ff907) keeps the same contract address, storage and existing methods. `name_status` distinguishes missing namespaces, missing Registrars, unregistered names, active names and expired records in one observation. Existing payment error codes remain compatible.

The [10 September batch optimization](https://stellar.expert/explorer/testnet/tx/5c444700a524c0a2454ad94cd3822562e7774a5d16c219e6c0599046d6910c91) keeps all contract addresses unchanged. `reverse_names` accepts up to **32** G/C/full-M identities in one namespace; `primary_names` accepts up to **16** across namespaces. Current native contracts combine related reads and share namespace verification work while still checking each live forward destination. Lookup 0.10.2 history helpers split calls when a host budget limit requires it and preserve each row's ledger/time. See [contract methods](/api/onchain-resolution#history-screens-and-multiple-addresses), [SDK examples](/sdk/reverse-and-primary#history-screens) and [exact JavaScript/XDR shapes](/reference/lookup-decoding#status-and-batch-shapes).

Live checks preserved the payment and elected-name answers for `nikcle.nova`, `robert.nova`, `mux.nova` and the forward-only contract example `cyclops.nova`. Generated TypeScript bindings also read the new methods successfully. Expiry boundaries and malformed dependency responses were checked in compiled-contract tests.

<span id="pending-allocator-upgrade" />

## Allocator upgrade

The Allocator records [case-specific rulings and explanations](/reference/namespace-objections) on chain. Code-upgrade execution and cancellation bind the exact reviewed code hash. Reopening cancellation and execution now bind the [exact proposal identity](/reference/namespace-objections#reopening-proposals), so a replacement proposal needs a new review. The [8 September upgrade](https://stellar.expert/explorer/testnet/tx/a9c77709a1a4e51fa4a5ca74647e332302f629ef53578bb3bc912fd7e29ca9c3) keeps the same contract address and storage schema `4`.

Those upgrades added case-specific rulings, removed the mandatory wait for new governance-approved code proposals and fixed XLM settlement to G accounts that do not yet exist. A payout can create the account when it meets the required reserve; if the transfer fails, the recipient receives a recoverable credit. The upgrades are recorded separately in the deployment manifest.

The 10 September governed migration also upgraded Allocator code in place and retained its escrow. Namespace claim and objection windows, the 5,000 XLM fee and refund entitlements remain unchanged. See [governance and upgrades](/concepts/governance#allocator-upgrade-flow).

## Hosted services

| Service | Address |
| --- | --- |
| Soran console | [soran.domains](https://soran.domains) |
| HTTP API | [api.soran.domains](https://api.soran.domains) |
| Resolution API alias | [resolve.soran.domains](https://resolve.soran.domains) |
| Hosted MCP, read-only | [mcp.soran.domains/mcp](https://mcp.soran.domains/mcp) |
| Nova claim app and Whois | [Open Nova](https://nova-claim-app.throbbing-cloud-0f8e.workers.dev/) |
| SDK demonstration app | [Open Soran Studio](https://soran-studio.throbbing-cloud-0f8e.workers.dev/) |

These services use the current deployment. Hosted MCP runs **0.9.3**; signing tools are [local-only](/agents/access#native-usernames-in-local-mode). Billing and marketplace features are disabled on this setup. Check [service status](/api/status) when integrating.

The 8 September console/API update checks [current namespace ownership](/platform/console#current-ownership-checks), preserves the transaction hash after an [interrupted submission](/api/overview#submission-responses), and lets you recover an interrupted renewal before attempting another. Owners can now [set all five activation rules](/owner/activating-a-namespace), including disabling holder transfers or setting a default expiry term. The API and local MCP accept the same explicit policy, preserve the two preset defaults, and verify the requested rules before signing. The console also checks a maximum network fee and a short transaction expiry before wallet approval. Its name-management page supports full muxed identities through Universal Lookup.

<span id="upcoming-updates" />

## Governed testnet deployment

The 10 September migration preserves the Universal Lookup and Allocator addresses. Registry, Primary and Nova's Registrar and Resolver use the successor addresses above. Match them with Lookup **0.10.2**, Owner **0.8.0**, Holder **0.7.0** and MCP **0.9.3**.

All six active contracts support authorized upgrades at the same address. Namespace child upgrades require the current namespace owner and protocol approval for the exact contract role and code hash. The governed testnet does not permit irreversible code locks. Name expiry and owner reclaim policy remain separate settings. See [governance](/concepts/governance).

The migration retains existing names, payment destinations, exact memo values and full muxed routing IDs. Display-name lookups verify the complete destination; an M destination is not a base-G election. Pending namespace applications retain their original claim windows, escrow and refund entitlements.

Keep original saved claims and transaction hashes. Owner and Holder can verify historical receipts through `recoverHistoricalClaim(originalIntent, { lookupId })` once canonical Lookup confirms the sealed successor. A missing or unavailable receipt does not authorize an automatic retry. Source receipts remain at their original Registrar.

New clients also enforce a shared network-fee ceiling for writes and storage restoration; see [client configuration](/sdk/installation#write-client-fee-limit).

## Historical records

Older deployments have separate state. Soran does not provide a supported legacy app or API route. For historical inspection, use the snapshots below and the [legacy storage reference](/reference/legacy-contract-storage):

- [9 September deployment, before the governed migration](https://raw.githubusercontent.com/SoranDomains/docs/main/reference/deployments/testnet-2026-09-09-pre-governed.json)
- [5 September native-muxed deployment](https://raw.githubusercontent.com/SoranDomains/docs/main/reference/deployments/testnet-2026-09-05-native-muxed.json)
- [5 September namespace-bound deployment](https://raw.githubusercontent.com/SoranDomains/docs/main/reference/deployments/testnet-2026-09-05-bound-v1.json)
- [Earlier unscoped deployment](https://raw.githubusercontent.com/SoranDomains/docs/main/reference/deployments/testnet-2026-09-05-unscoped.json)

Do not combine their contract addresses with the current setup. Stellar testnet can reset.
